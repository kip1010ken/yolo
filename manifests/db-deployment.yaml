apiVersion: apps/v1
kind: Deployment
metadata:
  name: yolo-mongo-db
spec:
  replicas: 1
  selector:
    matchLabels:
      app: yolo-mongo-db
  template:
    metadata:
      labels:
        app: yolo-mongo-db     
    spec:
      containers:
      - name: mongodb
        image: mongo:latest
        ports:
        - containerPort: 27017
        volumeMounts:
          - mountPath: /data/db
            name: mongo-storage
      volumes:
      - name: mongo-storage
        persistentVolumeClaim:
          claimName: app-mongo-data
